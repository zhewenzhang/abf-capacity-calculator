# Phase 6.2 下一批 CC / AGY 并行研发任务清单 (Product Backlog)

本文件列出了在 **ABF Capacity Calculator** 的 **Phase 6.2 Forecast Version History Workflow** 完成后，协同开发团队 (CC) 与 评估顾问团队 (AGY) 可以在后续并行迭代（Phase 6.3 / Phase 7）中开展的并列任务 backlog 清单。我们针对每个任务细分了具体的 Scope、只读属性、部署风险及推荐实施顺序，以期以最高效的并行步调推动项目演进。

---

## 一、 协同开发团队 (CC) 并列研发任务清单 (10 个任务)

协同开发团队后续应致力于通过确定性的功能打磨，强化多版本工作流的决策深度与本地化闭环。

| 任务编号 (Task) | 任务名称 (Task Name) | 详细业务 Scope 与交付指标 | 并行属性 (Parallel?) | 只读? | 部署/发版 风险等级 | 推荐实施 优先级 |
| :---: | :--- | :--- | :---: | :---: | :---: | :---: |
| **CC-01** | **i18n P3 硬编码补丁** | 将 `CalculationResults.tsx` 中 Statistic 的 `BP Gap Delta`（第 1869 行左右）与 `Max Core Util Delta`（第 1944 行左右）采用 `t()` 包裹提取，实现 100% paracitity 镜像对齐。 | 🟢 可并行 | ❌ 需改代码 | 🟢 低 (仅改前端文案) | **P0 (最优先)** |
| **CC-02** | **快照列表描述 Tooltip 展开** | 当快照 `description` 或 `Note` 备注长文本在表格内溢出时，引入高体验感的 AntD Tooltip 悬浮全量显示，防止页面表格高度变形。 | 🟢 可并行 | ❌ 需改代码 | 🟢 低 (仅优化样式) | **P1** |
| **CC-03** | **快照删除后级联联动重置** | 当用户删除了正在被 Base 或 Target 选中的活跃比对快照时，系统自动联动清空 Select 选择值并重置大盘为空状态，防止内存引用空快照抛错。 | 🟢 可并行 | ❌ 需改代码 | 🟡 中 (涉及状态清理) | **P1** |
| **CC-04** | **BP Gap 汇率多币别支持** | 扩展 `changeImpact.ts` 使得 BP Target 依然锚定 Million TWD，但在计算 GAP 时，大盘能自动读取换算率，在副标题或小字中额外展示对应的 USD/CNY Gap，提升决策友好度。 | 🟢 可并行 | ❌ 需改代码 | 🟡 中 (涉及公式计算) | **P2** |
| **CC-05** | **一键 Apply 比对对的键盘快捷键** | 为 Recommended compare pairs 推荐卡片的「套用推荐」按钮，绑定全局快捷键（如 `Ctrl + Enter`），方便产能专家用键盘无鼠标流秒开大盘比对。 | 🟢 可并行 | ❌ 需改代码 | 🟢 低 (仅前端事件绑定) | **P2** |
| **CC-06** | **空状态友好新手引导丰富** | 当快照数量为空时，在 Empty 状态中渲染出带精美 SVG 插画以及步骤引导的卡片，点击可一键自动填充一组“沙箱 Demo 快照数据”以供新手试用。 | 🟢 可并行 | ❌ 需改代码 | 🟢 低 (纯静态引导) | **P3** |
| **CC-07** | **本地 JSON 离线多层级校验** | 优化 `changeImpactExport.ts` 中 `validateChangeImpactPack` 对导入的 JSON 文件结构健全性的深度递归判定，防止导入被非法篡改的代码包发生运行时报错。 | 🟢 可并行 | ❌ 需改代码 | 🟢 低 (纯工具类优化) | **P3** |
| **CC-08** | **快照创建者 CreatedByName 离线缓存** | 在建立快照时，自动读取本地 LocalStorage 或 Preferences 中的当前用户名缓存作为 `createdByName` 的兜底 fallback，彻底消除“未知创建者”的情况。 | 🟢 可并行 | ❌ 需改代码 | 🟢 低 (纯本地缓存读取) | **P3** |
| **CC-09** | **最大核心稼动率波动 10% 警报变色** | 细化 `CalculationResults.tsx` 中 `Max Core Util Delta` 的警示颜色梯度。当波动 pp 差值在 5% 以内时显示为灰色，5%-10% 之间显示为橙色，超过 10% 显示为象征危急的红色。 | 🟢 可并行 | ❌ 需改代码 | 🟢 低 (纯样式条件改动) | **P3** |
| **CC-10** | **Vitest 单元测试覆盖率拉升至 90%** | 为 `changeImpactExport.test.ts` 以及新增的元数据辅助函数，编写多路参数边界覆盖单元测试，确保回归验证极其坚固。 | 🟢 可并行 | ❌ 需改代码 | 🟢 低 (仅改测试文件) | **P2** |

---

## 二、 评估顾问团队 (AGY) 并列研发任务清单 (10 个任务)

评估顾问团队后续应致力于通过构建高质量的旁路测试体系、多角色安全加固评测以及大模型提示词优化，协助项目平稳前行。

| 任务编号 (Task) | 任务名称 (Task Name) | 详细业务 Scope 与交付指标 | 并行属性 (Parallel?) | 只读? | 部署/发版 风险等级 | 推荐实施 优先级 |
| :---: | :--- | :--- | :---: | :---: | :---: | :---: |
| **AGY-01** | **Scenario 推荐比对算法设计** | 提前为 Phase 6.3 规划“多分支 Scenario 快照比对推荐算法白皮书”。规划如何自动配对“情境A 与 情境B”的衍生差异。 | 🟢 可并行 | 📇 纯只读 (文档) | 🟢 低 (0 部署风险) | **P1 (最优先)** |
| **AGY-02** | **Firestore 本地规则自动化测试** | 将手动冒烟脚本中的跨角色越权漏洞测试 (TC-01 至 TC-05) 翻译并固化为 Vitest 本地 Local Rules Emulator 物理自动化测试用例，实现云端防火墙的流水线回归。 | 🟢 可并行 | 📇 纯只读 (测试) | 🟢 低 (不改 rules, 只增测试) | **P1** |
| **AGY-03** | **DeepSeek 价格与数量效应评分卡细化** | 针对 DeepSeek 大模型对“营收 Delta 物理一阶价格/数量归因”的拆解能力，设计包含 10 个加权载板业务用例的深度 benchmark 评分白皮书。 | 🟢 可并行 | 📇 纯只读 (评分卡) | 🟢 低 (0 部署风险) | **P2** |
| **AGY-04** | **双语镜像 Parity 自动化扫描脚本** | 在 `docs/` 下编写一个一键扫描脚本，自动用正则核查 `en.ts` 与 `zhTW.ts` 中的所有 key 数量及命名是否完全一致，在 CI 中实现 i18n 破损零容忍。 | 🟢 可并行 | 📇 纯只读 (脚本) | 🟢 低 (独立评估脚本) | **P2** |
| **AGY-05** | **红队越权漏洞只读评估白皮书** | 基于 Firestore Immutable 规则红线，对目前快照的云端 Collection 防拦截做一次深度渗透漏洞审查，输出硬性的防御评级。 | 🟢 可并行 | 📇 纯只读 (文档) | 🟢 低 (0 部署风险) | **P1** |
| **AGY-06** | **AI Export Sanitization 规则审计** | 穿透审计当前导出的 JSON 底料数据，查核是否仍残留有任何极隐蔽的客户商誉、敏感组织名称等，进一步加固脱敏物理边界。 | 🟢 可并行 | 📇 纯只读 (文档) | 🟢 低 (0 部署风险) | **P1** |
| **AGY-07** | **真实排产专家习惯评测计划** | 制定一份针对大厂产能规划经理和业务线总监的使用反馈访谈计划，收集他们在进行 Forecast 季度微调时对 UI 版本流检索的眼动和心智体验反馈。 | 🟢 可并行 | 📇 纯只读 (文档) | 🟢 低 (0 部署风险) | **P3** |
| **AGY-08** | **多币别百万台币换算逻辑安全盘点** | 针对系统底层的 `currency.ts` 和汇率 Parameters 映射做一次只读对账审计，核实有无任何换算浮点数偏差，并出具安全防漂移盘点报告。 | 🟢 可并行 | 📇 纯只读 (文档) | 🟢 低 (0 部署风险) | **P2** |
| **AGY-09** | **大模型 R1 提示词 Pack 架构演进** | 提前为 R1 (DeepSeek 推理大模型) 设计优化其 Prompt。利用 R1 的 `<think>` 长链推理，使其更精确地辨识出“事实/归因/推论/建议”四层级。 | 🟢 可并行 | 📇 纯只读 (Prompt) | 🟢 低 (0 部署风险) | **P2** |
| **AGY-10** | **协同工作区共享权限边界二次审计** | 在 `docs/security/` 下，针对 Workspace Owner、Editor、Viewer 在多版本控制和 Scenario 分叉操作下的数据越权泄漏隐患做一次深度合规安全审计。 | 🟢 可并行 | 📇 纯只读 (文档) | 🟢 低 (0 部署风险) | **P2** |
